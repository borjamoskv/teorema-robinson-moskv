# C5-REAL EXERGY CERTIFIED
[package]
name = "kudurru_64"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
wasm-bindgen = "0.2"

[profile.release]
# Optimize for size and speed (Ω15)
lto = true
opt-level = 3
codegen-units = 1
