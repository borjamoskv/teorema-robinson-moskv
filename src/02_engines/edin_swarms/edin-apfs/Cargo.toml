# C5-REAL EXERGY CERTIFIED
[package]
name = "edin-apfs"
version = "0.1.0"
edition = "2021"
authors = ["Borja & Exergy Swarm Team"]
description = "Bulk APFS scanner con getattrlistbulk(2) a 64KB y auditor de snapshots"

[dependencies]
edin-core = { path = "../edin-core" }
libc = "0.2"
serde = { version = "1.0", features = ["derive"] }
sha3 = "0.10"
