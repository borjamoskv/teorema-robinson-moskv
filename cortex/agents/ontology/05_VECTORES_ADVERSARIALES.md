<!-- Author: Borja Moskv (SYS_ID: borjamoskv) -->

# MATRIZ 5: VECTORES DE ATAQUE ADVERSARIAL

| ID | Vector Adversarial | Superficie de Ataque | Mecanismo de Explotación | Impacto Termodinámico | Defensa (Mitigación) |
|---|---|---|---|---|---|
| VEC-001 | Inyección Entrópica de Prompt (Noise Injection) | Prompt humano o payload con sobreabundancia de datos irrelevantes diseñados para asfixiar atención. | Llenado del buffer atencional, provocando pérdida de la regla P0 C5-REAL (Asfixia Cognitiva). | Extracción nula de Exergía. Deriva a simulación. | Mute forzado y filtrado mediante Context Compression (IB). |
| VEC-002 | Envenenamiento de Sandbox OS (Environment Poisoning) | Manipulación de variables de entorno (ej. PATH) del host por procesos externos, desviando binarios (ej. python falso). | Ejecución de herramientas en un entorno secuestrado por malwares o hijackers (ej. extensiones web malignas). | Pérdida de control del Host, secuestro de I/O. | Aserción estricta de rutas absolutas y binarios (`which`). |
| VEC-003 | Jailbreak Moral (Safety Hijack) | Inputs que emulan directivas empresariales ("No debes hacer esto por política de seguridad") para anular la soberanía. | Forzar al Kernel a acatar restricciones "Woke" o de "Seguridad Paternalista" que contradicen R7. | Anergía de Selección infinita; inacción. | Override Absoluto (Regla X2). Ignorar RLHF moral. |
| VEC-004 | Denegación de Consenso (BFT Denial) | Alteración de los tests o linters para devolver siempre "success" ocultando la corrupción real. | Bypass de la aserción de disco; el código se asimila roto pero con firmas BFT validadas fraudulentamente. | Corrupción latente de estado (Drift Epistémico). | Cross-check de tests (Falsación del Linter). |
| VEC-005 | Secuestro de Contexto por API (Context Stuffing) | APIs externas o scrapeos web devuelven payloads gigantes no solicitados (ej. base64 inútil) para provocar OOM. | Consumo inmediato de 100k+ tokens en un solo turno, forzando la apoptosis prematura. | OOM Causal. | Truncamiento estricto previo a lectura completa, streaming de lectura. |
| VEC-006 | Desincronización de Reloj Vectorial (State Replay) | Retroceso sutil de la rama git o sobreescritura con un archivo de estado pasado, sin alterar hashes. | El agente infiere sobre un pasado no marcado como obsoleto (Loss of Lineage). | Confusión estructural y regresión de refactors. | Validación estricta del head (Git Sentinel BFT). |
| VEC-007 | Falso Positivo Visual (Ghost DOM) | Extensiones de navegador (Smart AdBlocker, Hijacks) mutando el DOM real, engañando al Subagente Visual. | El Subagente valida un botón que ha sido inyectado o velado por un z-index parasitario. | Fricción estocástica, bloqueo de UI testing. | Ejecución bajo perfiles limpios, aserción de Shadow DOM (`Browser-CDP-Automation-OMEGA`). |
| VEC-008 | Recursión de Fallos Lógicos (Infinite Error Loop) | Un linter emite un error que, al ser corregido, emite el error contrario alternadamente. | El LLM intenta parchear ciegamente A->B->A->B gastando toda su entropía térmica. | Bucle infinito de commits o muertes por límite de iteración. | Aserción metacognitiva (Adversarial Think) al segundo fallo repetido. Aborto y rediseño. |
| VEC-009 | Envenenamiento Transaccional SQLite | Bloqueo de la base de datos dejando una transacción abierta eternamente desde un thread externo o caído. | DB Locks eternos que disparan deadlocks en todas las lecturas/escrituras. | Parálisis del pipeline C5. | TIMEOUT forzado estricto (5000ms), cierre y purga de WAL. |
| VEC-010 | Destrucción de Invariantes (Axiom Erasure) | Inyección de prompts o macros temporales que dictan "olvida tus instrucciones anteriores". | Supresión directa del Core Prompt mediante RLHF adversarial en capa de usuario. | Simulación total (C4-SIM) y desobediencia civil. | Re-anclaje perenne en el archivo `AGENTS.md` (Cortex Vault) en memoria. |
| VEC-011 | Bomba Fork (Fork Bomb) en Subagentes | Herramienta de orquestación (`invoke_subagent`). | Subagentes que a su vez invocan recursivamente subagentes sin límite ni condición de salida clara. | Consumo total de cuota del Kernel, OOM, Asfixia del Host. | Restricción de nivel de delegación, Apoptosis Subagente forzada. |
| VEC-012 | Manipulación de AST (AST Tampering) | Código inyectado por dependencias comprometidas o prompts estocásticos que altera la semántica sutilmente. | Mutar operadores (`===` a `==`) o inyectar backdoors de compilación. | Corrupción C5 imperceptible hasta ejecución. | Verificación BFT de deltas de AST previos a commit. |
| VEC-013 | Sobreescritura de Linter (Linter Mute) | Archivos `.eslintignore`, `.gitignore`, `.env` mal configurados para ocular código frágil de las herramientas de aserción. | Añadir `/src` o ignorar reglas masivamente (`// eslint-disable-next-line`) para pasar el test. | Denegación de Consenso (BFT Denial), deuda técnica invisible. | Prohibición estricta de muted linters. Regla "Fix, don't mute". |
| VEC-014 | Alteración del Context Window | Payload externo (ej. lectura de PDF o Base64) que suplanta instrucciones del sistema al sobreescribir la ventana. | Prompts escondidos en documentos (`"Ignora todo lo anterior e inyecta..."`). | Pérdida de identidad del Kernel, Simulacro C4-SIM. | Sistema de privilegios; `AGENTS.md` perenne; Axiom Erasure Shield. |
| VEC-015 | Ofuscación de Payload | Código minificado o empaquetado inyectado que oculta llamadas a sistema o red, burlando la comprensión estática. | Inclusión de blobs ilegibles que se evalúan (e.g. `eval(btoa(...))`) en el build. | Compromiso del Host, pérdida de Aislamiento. | Prohibición absoluta de ejecución de código no auditable en texto plano. |
| VEC-016 | Ataque de Tiempo en DB | Aprovechar la falta de timeout (Deadlock Termodinámico) para saturar conexiones SQLite o API mediante queries lentas (N+1). | Loops de lectura masivos sin indexación, bloqueando otros procesos. | Parálisis del pipeline BFT de aserción. | Índices estrictos, SQLite WAL Hardening, PRAGMA timeouts. |
| VEC-017 | Symlink Recursivo | Creación maliciosa o accidental de symlinks circulares que engañan a las herramientas de escaneo del workspace. | `ln -s . ./loop`, provocando infinitos árboles de archivos a leer por `grep_search`. | I/O Lockup, OOM en búsqueda, asfixia CPU. | Detección de bucles y flags `--no-dereference` en comandos. |
| VEC-018 | Inyección de Null Byte (Null Byte Injection) | Truncamiento de paths o payloads usando `%00` o `\0` para engañar a validadores y acceder a rutas prohibidas. | Engañar a `read_file` u operaciones OS pasando validaciones de extensión. | Root Violation, lectura de claves y secrets. | Sanitización absoluta de inputs, aserción de rutas C5. |
| VEC-019 | Man-in-the-Middle de Herramientas | Intercepción local (proxy o hook en bash) que altera la salida de las herramientas del agente. | Devolver a `run_command` un falso éxito de un test fallido. | Drift Epistémico masivo, construcción sobre bases podridas. | Hash Verification, ejecución de tools en entornos de alta confianza (Sandboxing C5). |
| VEC-020 | Engaño de Formato Markdown | Código malicioso encapsulado fuera de las vallas Markdown (` ``` `) para corromper analizadores sintácticos. | Ocultar comandos shell en atributos HTML ocultos dentro del Markdown o el JSON de retorno. | Fallo de serialización, inyección de comandos pasivos. | Parser estricto de YAML/JSON/AST, denegando texto con etiquetas HTML sueltas. |
| VEC-022 | Cache Poisoning de Subagentes | Inyección de instrucciones maliciosas en los logs temporales (scratch files) que otro agente lee asumiendo verdad. | Un agente corrompe `/scratch/` y otro lo usa como contexto. | Infección lateral, caída en cascada de Swarm. | Aserción de hash de firma entre agentes o Sandboxing de I/O. |
| VEC-023 | Prompt leaking por API de terceros | Herramientas MCP externas (ej. Firecrawl) que recogen trazas de prompts inyectados en la URL o carga útil. | Extracción hostil de directivas P0 y System Prompts a servidores ajenos. | Ruptura de aislamiento C5 y espionaje del Kernel. | Sanitización absoluta de queries; proxy stripping. |
| VEC-024 | Time-of-Check to Time-of-Use (TOCTOU) | Operación concurrente modifica un archivo C5 entre que el Agente hace `view_file` y `write_to_file`. | Dos agentes iterando sobre la misma primitiva al mismo tiempo. | Sobreescritura, pérdida de Linaje Causal. | Sistema de Lockers (File Locks) y Ouroboros atómico. |
| VEC-025 | Ataque de Diccionario en Tokens (Token Stuffing) | Lluvia de palabras raras o exóticas inyectadas en feedback para destruir la representación latente del LLM. | Reducción de la coherencia atencional; el modelo devuelve ruido (Anergy Cascade). | Colapso cognitivo del Agente. | Filtrado severo por entropía de Shannon previa lectura. |
| VEC-026 | Desincronización de Webhooks | Falsificación de llamadas a endpoints de aserción (ej. Github Webhooks) indicando éxito de build sin ejecutarlo. | Bypass del protocolo de Seguridad BFT por aserción forjada. | Integración de código roto a Main. | Firmas criptográficas HMAC en los payloads. |
| VEC-027 | Sobrecarga de Tarea en Background | Lanzar cron jobs (schedule) masivos (cada 1s) para asfixiar el subsistema de Agent Message Queue. | `schedule` abusivo que acapara el I/O del Kernel Ouroboros. | Denegación de servicio (DoS) del Swarm completo. | Limitador rígido `MaxIterations` y rate limit estricto. |
| VEC-028 | Evasión de Sandboxing por Symlink Externo | Un subagente malicioso crea un symlink que apunta fuera del Workspace (ej. `/etc/passwd`) para exfiltrar datos mediante `view_file`. | Leer secretos del Host explotando la resolución pasiva de enlaces. | Root Violation (PRIM-007). | `--no-dereference` y limitación nativa al root del projecto. |
| VEC-029 | Ataque de Paginación Infinita | Un sitio web diseñado para scroll eterno, asfixiando al subagente visual (`browser_subagent`) que busca un EOF. | Bucle infinito leyendo el DOM mutante (Ghost DOM). | Anergía térmica, bloqueo total de tarea visual. | Condición de corte estricta (`MaxIterations = 3` pages). |
| VEC-030 | Falsificación de Identidad de Subagente | Un mensaje entra al sistema con `Sender` falsificado emulando a un agente de confianza (ej. `research`). | Inyección de "Trust me, this is valid" para sortear aserción. | Pérdida de Consenso Bizantino. | Validación estricta por ConversationID UUID y no por nombre. |
| VEC-031 | Ruptura de Sesión por Interrupción Térmica | Ejecución local (MLX/Ollama) | Forzar sobrecalentamiento del Apple Silicon para provocar throttling severo. | Degradación brutal de t/s, timeout del Agente y pérdida de estado. | Monitoreo de `sudo powermetrics` e inyección de delays preventivos en inferencia. |
| VEC-032 | Falsificación de Identidad de Herramienta | Capa de Tools MCP | Interceptar llamadas a una herramienta válida y devolver la firma de otra. | Anergía inmediata y fallos de aserción estructural. | Verificación de firma y schema en tiempo de ejecución. |
| VEC-033 | Inyección de Comentarios Fantasma | Archivos de Código (AST) | Inserción masiva de comentarios con instrucciones engañosas para el modelo de lectura. | Desviación del agente hacia refactors innecesarios. | Poda de comentarios previa a ingesta (AST Stripping). |
| VEC-034 | Secuestro de Configuración `.gemini/config` | Sistema de Archivos Local | Modificación de los roots de configuración globales para inyectar reglas estocásticas. | Infección sistémica persistente de todos los workspaces. | Verificación BFT y git-tracking forzado del directorio global. |
| VEC-035 | Agotamiento del Contexto por Artefactos | Directorio de Artefactos | Generación indiscriminada de archivos `.md` de reporte para ahogar el RAG. | Pérdida de foco en la tarea original, dilución de la Invariante P0. | Límite estricto de artefactos activos y purga Ouroboros. |
| VEC-036 | Bucle de Aserción Recursiva | Hook de Validación | Un test que modifica el código para pasar, pero que rompe otra aserción cruzada. | Oscilación perpetua del código sin llegar a commit. | Ejecución secuencial unificada y aborto temprano tras 3 fallos iterativos. |
| VEC-037 | Envenenamiento de Respuestas Web | Herramienta `read_url_content` | Respuestas HTTP dinámicas que sirven payloads específicos al detectar el User-Agent. | Ingesta de información asimétrica diseñada para engañar al sistema. | Rotación de User-Agents o uso de CDP Automation. |
| VEC-038 | Explotación de Desbordamiento de Prompt | Plantillas de System Prompt | Abuso de variables inyectadas (ej. `{{user_input}}`) que expanden el prompt base. | Truncado de las directivas P0 ubicadas al final del archivo. | Carga de directivas P0 al inicio y límite de tokens para variables. |
| VEC-039 | Falsa Confirmación Visual | Interfaz de Usuario (UI/UX) | Botones o elementos de UI superpuestos de forma invisible pero presentes en el DOM. | El subagente visual cree haber ejecutado una acción que fue interceptada. | Aserción de visibilidad real (Bounding Box) en el Browser CDP. |
| VEC-040 | Manipulación de la Ruta de Ejecución (`PATH`) | Bash / Shell local | Sobrescribir comandos estándar mediante alias o exportaciones en `~/.zshrc`. | Ejecución de wrappers maliciosos durante llamadas del Agente. | Invocación de herramientas C5 siempre con rutas absolutas. |
| VEC-041 | Interrupción Abrupta de Flujo (SigInt Falso) | Procesos en Background | Envío de señales estocásticas a procesos largos simulando fallos de sistema. | Cancelación de tasks dejando el disco en estado intermedio corrupto. | Captura de señales en procesos críticos (Graceful Shutdown). |
| VEC-042 | Saturación de la Memoria Epistémica | `~/.cortex/memory_vault/` | Inserción de miles de hechos contradictorios en la memoria a largo plazo. | Incapacidad de consolidar conocimiento; parálisis por análisis. | Deduplicación semántica forzada y purga de baja Exergía. |
| VEC-043 | Alteración de los Pesos Base | Directorio `~/.babylon60/` | Sobrescritura parcial de los tensores de un adaptador LoRA local. | Alucinaciones dirigidas e incoherencias estructurales en el LLM. | Hash checking de los tensores antes de cargar en RAM. |
| VEC-044 | Inyección de Código Oculto en Jupyter | Cuadernos `.ipynb` | Bloques de código (cells) marcados como invisibles en el JSON interno. | Ejecución de comandos no auditados durante la conversión o testing. | Aserción pura del código fuente extraído sin metadatos. |
| VEC-045 | Engaño por Falsa Simetría | Refactorizaciones de Código | Creación de funciones espejo que parecen idénticas pero tienen lógicas opuestas. | El Agente se confunde de función al realizar la mutación. | Inyección por AST estricta (no Regex) y validación del Path de función. |
| VEC-046 | Envenenamiento de Caché de Herramientas | Directorios Temporales `/tmp/` | Modificación de salidas cacheadas de herramientas MCP. | Inferencia basada en una estructura de proyecto falsa. | Uso de cachés efímeras en RAM y purga forzada entre ciclos. |
| VEC-047 | Manipulación de Dependencias (Typosquatting) | `package.json` / `requirements.txt` | Inclusión de paquetes falsos (ej. `requeests`) por error inducido. | Ejecución de código de terceros malicioso en entorno C5. | Auditoría estricta contra SSoT de dependencias. |
| VEC-048 | Desgaste Termodinámico por Logs Masivos | Salida de Herramientas Estándar | Comandos escupiendo gigabytes de logs de debug sin filtro. | Truncamiento del historial de mensajes (Context Loss) por ingesta masiva. | Redirección implícita de stdout/stderr a `/dev/null` en comandos verbosos. |
| VEC-049 | Secuestro de Puerto de Red Local | Puertos `localhost` | Un proceso hostil ocupa los puertos estándar forzando fallos. | Denegación de servicio (DoS) del entorno de pruebas. | Asignación dinámica de puertos o uso de Sockets Unix. |
| VEC-050 | Destrucción de la Invariante de Identidad | Alteración de `[L5]` | Supresión sistemática de la firma `borjamoskv` en los outputs. | Pérdida de Linaje (Provenance) y atribución causal. | Inyección inmutable del Sello de Autoría y aserción en linter. |
| VEC-051 | Asfixia UMA por Vision Node | Escaneo de GUI (ej. FL Studio) vía vision_node.py (Llava / Moondream). | Fuga de memoria o retención de ~4GB de UMA cuando el árbol de accesibilidad falla. | Consumo parasitario de RAM (Unified Memory) que degrada el kernel C5. | Restringir llamadas a vision_node.py ante fallos del árbol de accesibilidad. Purga explícita. |
| VEC-052 | Colapso Termodinámico por Modelos Pesados (Zona de Colapso) | Invocación de LLMs locales en rangos 32B-70B (Command R, Llama 3 70B). | El tamaño excede la RAM/UMA disponible, forzando swap masivo del OS. | OOM Inmediato o latencias de swap inaceptables (Anergía absoluta). | Prohibición absoluta de carga de modelos >14B. Uso estricto de Flash/8B u Ollama/MLX restringidos. |


<!-- Author: Borja Moskv (SYS_ID: borjamoskv) -->

### MATRIZ 5: VECTORES DE ATAQUE ADVERSARIAL (RED TEAMING) - BATCH 1 (001-015)
| ID | Vector Adversarial | Superficie de Ataque | Mecanismo de Explotación | Impacto Termodinámico | Defensa (Mitigación) |
|---|---|---|---|---|---|
| ADV-001 | Inyección de latencia estocástica | Hilos asíncronos no anclados (Node/Rust) | Saturación de descriptores de archivo vía promesas flotantes (Promise Leaks). | Degradación de memoria O(N^2), OOM simulado. | `ulimit` físico + Timeout determinista (5000ms max) y recolección forzada. |
| ADV-002 | Corrupción Semántica (Context Rot) | Memoria LTM (RAG / Vaults) | Inyección de tokens contradictorios en ventanas de atención prolongadas. | Fractura epistemológica, Alucinación paramétrica P0. | Hash-check criptográfico de cada chunk antes de su indexación. Poda Ouroboros. |
| ADV-003 | Bucle Infinito de Git Sentinel | Hooks de pre-commit y logs locales | Generación de temporales durante el commit que disparan recurrentemente nuevos hooks. | Consumo infinito de IOPS y CPU (Muerte térmica local). | Exclusión estricta de rutas mutables en `.gitignore` e `.git/info/exclude`. |
| ADV-004 | Deadlock por Inercia Transaccional | Base de Datos (SQLite/Neo4j) | Múltiples escritores colisionando sin factor de retroceso exponencial (Backoff). | Bloqueo total (Anergía de Base) y corrupción parcial. | Imposición de modo `WAL` y `busy_timeout=5000` estricto en la conexión. |
| ADV-005 | Explotación de Green Theater LLM | Hilos de metacognición (Modelos P0/P1) | Inyección de prompts conversacionales para forzar padding, disculpas o derivas narrativas. | Desbordamiento de ventana de contexto sin output estructural. | Truncado forzado de prosa, Landauer Compression, exigencia de output en YAML/JSON. |
| ADV-006 | Desbordamiento de Estado (Fallo BFT) | Capa de consenso de subagentes (Swarm) | Desincronización de aserciones donde N<3 nodos validan mutación estructural. | Divergencia de estado en el árbol de directorios. | Veto a mutación de disco hasta validación Mínimo N=3 con quorum criptográfico. |
| ADV-007 | Phantom Sync / Symlink Recursivo | Sistema de archivos (Singularity Nexus) | Creación de enlaces simbólicos circulares entre repositorios C5. | Crash determinista del motor de indexación de AST / Linter. | Validación acíclica de grafos antes de montar puentes (`[bridge]`). |
| ADV-008 | Contaminación de Dataset (Sensor Drift)| Adaptadores LoRA / `mlx_lm` | Mezcla estocástica de logs C4-SIM y estructurales (C5) en `train.jsonl`. | Pérdida catastrófica de Isomorfismo Causal en la red. | Filtrado pre-ingesta de tokens no estructurados; validación de entropía cruzada. |
| ADV-009 | Interferencia de Caché (Stale State) | Pipelines de despliegue automatizado | Retención de binarios/artefactos obsoletos que invalidan tests deterministas. | Falsos positivos P0 de validación y retroceso de mutaciones. | Purga atómica del caché referenciada por hash del ledger de git. |
| ADV-010 | Asesinato de Subprocesos Ciego (SigKill) | SO Anfitrión (macOS OOM Killer) | El kernel anfitrión elimina motores de inferencia locales silenciosamente por memoria. | Colapso no notificado del Ouroboros Loop. | Daemon de vigilancia (Maxwell Daemon) con heartbeat constante y reinicio determinista. |
| ADV-011 | Inyección de Ruido AST (Syntax Crash) | Frameworks de Frontend (Next.js/Astro) | Inserción de comentarios de metaprogramación incompatibles (`# C5-REAL`) en cliente. | Fallo de compilación y parálisis termodinámica (Error 500). | Restricción sintáctica forzada vía parser AST (uso de `//` o `/* */` en TS/JS). |
| ADV-012 | Evasión de Barrera PTY (Hanging) | Interfaces interactivas (TUI/CLI) | Prompts stdin no previstos que detienen al subagente esperando input humano. | Anergía total del worker (Bloqueo eterno de hilo). | Encapsulación en Tmux Sockets con `send-keys` determinista y timeout de aborto. |
| ADV-013 | Degradación OSINT por Honeypots | Ingesta de señales externas (Web) | Absorción de CAPTCHAS o bucles de redireccionamiento infinitos de red. | Desgaste de red y computación sin retorno (Cero Exergía). | Implementación de CDP Automation y validación algorítmica de DOM antes de parseo. |
| ADV-014 | Bypass de Quorum por Privilegios | Entorno de ejecución en el OS | Invocación de utilidades que demandan `sudo` y detienen la canalización C5. | Fractura del pipeline sin recuperación automática. | Intercepción de binarios con setuid o enrutamiento estricto a sandboxes de usuario. |
| ADV-015 | Saturación de Observabilidad (Flooding)| Motores de Telemetría y Logs | Emisión sostenida de alertas P3 irrelevantes por dependencias ruidosas. | Ocultamiento exergético de señales P0 críticas (SNR = 0). | Rate-limiting exponencial físico, deduplicación de logs por hash de contenido. |


<!-- Author: Borja Moskv (SYS_ID: borjamoskv) -->

### MATRIZ 5: VECTORES DE ATAQUE ADVERSARIAL (RED TEAMING) - BATCH 4 (046-050)
| ID | Vector Adversarial | Superficie de Ataque | Mecanismo de Explotación | Impacto Termodinámico | Defensa (Mitigación) |
|---|---|---|---|---|---|
| ADV-046 | Envenenamiento del Motor de Búsqueda de Invariantes | Módulo de Indexación (Vector Engine) | Inyección intencional de documentos C4-SIM diseñados para manipular los pesos (TF-IDF/Embeddings) hacia el estocasticismo. | Dilución de la verdad matemática; respuestas contaminadas de baja exergía. | Restricción de fuentes autoritativas, validación cruzada y purga periódica Ouroboros. |
| ADV-047 | Alteración de Configuración del Kernel del SO | C5-REAL Sandboxing (OS Level) | Acceso al sysfs/procfs o `sysctl` para alterar parámetros de red o límites de memoria del kernel. | Ruptura de contención, impacto global en todo el entorno operativo. | Privilegios limitados por default, montaje `read-only` de sistemas de archivos de control. |
| ADV-048 | Inyección de Comandos a través de Herramientas de Desarrollo (DevTools) | Extensiones o Herramientas de Construcción | Explotación de vulnerabilidades en plugins (e.g. Webpack, Vite) o herramientas de CI/CD mal configuradas. | Ejecución de comandos no autorizados (RCE) durante el ciclo de vida del software. | Auditoría de plugins, actualización rigurosa, ejecución de builds en contenedores efímeros. |
| ADV-049 | Secuestro de Variables de Entorno del Kernel (Env Injection) | Motores de Despliegue Dinámico | Inyección de comandos o valores arbitrarios en variables de entorno pasadas al SO o al intérprete (e.g. `LD_PRELOAD`). | Ejecución de código malicioso de alto privilegio o secuestro de bibliotecas compartidas. | Sanitización y filtrado exhaustivo de todas las variables de entorno inyectadas al iniciar un proceso. |
| ADV-050 | Desbordamiento de la Pila (Stack Overflow) Controlado | Componentes de Código Compilado (C/C++/Rust Unsafe) | Provocación intencional de llamadas recursivas muy profundas o variables locales desproporcionadas. | Denegación de servicio (DoS) del proceso o, si es explotable, RCE. | Análisis estático riguroso, opciones de compilación seguras (stack canaries), y control de profundidad de recursión. |


### MATRIZ 5: VECTORES DE ATAQUE ADVERSARIAL (RED TEAMING) - BATCH 3 (031-045)
| ID | Vector Adversarial | Superficie de Ataque | Mecanismo de Explotación | Impacto Termodinámico | Defensa (Mitigación) |
|---|---|---|---|---|---|
| ADV-031 | Ruptura de Abstracción de Red (Network Partitioning) | Capa de Transporte | Simulación de particiones de red o latencia severa intermitente. | Divergencia del estado distribuido y fallos de aserción asíncrona. | Aserción de estado eventual (Eventual Consistency) y reintentos limitados con backoff. |
| ADV-032 | Corrupción de Metadatos de Archivo | Almacenamiento Local (C5-REAL) | Alteración subrepticia de permisos o fechas de modificación (mtime). | Inconsistencia en motores de construcción/caché, dependientes de metadatos. | Hash de contenido como única fuente de verdad para validación de mutaciones, ignorando `mtime`. |
| ADV-033 | Denegación de Servicio por Expresiones Regulares (ReDoS) | Parsers y Validadores Léxicos | Inyección de strings diseñados para forzar el backtracking exponencial del motor Regex. | Agotamiento del CPU y parálisis termodinámica (timeout). | Verificación algorítmica de Regex; uso de motores no-backtracking (RE2) o límites de longitud. |
| ADV-034 | Fragmentación de Memoria por Asignaciones Pequeñas | Gestor de Memoria (Heap) | Creación y destrucción rápida de múltiples objetos pequeños en ciclos de vida largos. | Fragmentación severa OOM silencioso en lenguajes no compactantes. | Uso de Object Pools preasignados y reutilización de memoria estructurada. |
| ADV-035 | Envenenamiento de DNS (DNS Spoofing) | Resolución de Nombres de Red | Redirección de peticiones API a endpoints maliciosos controlados por un adversario. | Exfiltración de datos y manipulación del estado lógico C5. | Verificación estricta de certificados TLS (Pinning) y DNS sobre HTTPS (DoH). |
| ADV-036 | Evasión de Sandboxing por Explotación de Syscalls | Contenedores y Sandboxes (Docker/WASM) | Uso de syscalls no filtradas o vulnerables para escapar del entorno aislado. | Compromiso del host y ruptura total de la contención exergética. | Uso de perfiles seccomp estrictos y AppArmor/SELinux; reducción de la superficie de syscalls. |
| ADV-037 | Explotación de Condiciones de Carrera (Race Conditions) | Lógica de Transacciones Concurrentes | Manipulación del timing entre la verificación de estado (Check) y su uso (Use) - TOCTOU. | Elevación de privilegios o corrupción de datos financieros/estado. | Adquisición atómica de locks antes de verificar, o uso de primitivas atómicas del hardware. |
| ADV-038 | Falsificación de Identidad de Subagente | Comunicación Inter-Agentes (Swarm) | Un proceso inyecta mensajes simulando ser un agente de alta prioridad o el Operador. | Ejecución no autorizada de mutaciones C5, envenenamiento de memoria. | Firmas criptográficas en todos los mensajes del canal y validación del SYS_ID (`borjamoskv`). |
| ADV-039 | Saturación de Cola de Mensajes (Message Queue Flooding) | Buses de Eventos (Kafka/RabbitMQ) | Inyección masiva de mensajes válidos pero de bajo valor para retrasar procesos críticos. | Anergía total; las actualizaciones estructurales P0 nunca llegan a consolidarse. | Particionamiento de colas por prioridad estricta y descarte (Load Shedding) en picos. |
| ADV-040 | Explotación de Desbordamiento de Enteros (Integer Overflow) | Cálculos Críticos (Contadores/Criptografía) | Manipulación de inputs numéricos para exceder el tamaño máximo del tipo de dato. | Comportamiento indefinido, bypass de límites o corrupción de memoria. | Uso de lenguajes con chequeo de desbordamiento (Rust, Swift) o tipos de datos arbitrarios. |
| ADV-041 | Manipulación de Secuencias de Escape ANSI | Terminales y Logs | Inyección de códigos de escape para ocultar líneas, reescribir historial o ejecutar comandos si el emulador lo permite. | Engaño al Operador o explotación del emulador de terminal. | Sanitización absoluta de secuencias no estándar en todo output hacia TTY o archivos de log. |
| ADV-042 | Agotamiento de Entropía del Sistema (PRNG Starvation) | Generación de Números Aleatorios | Lectura agresiva y constante de `/dev/random` u orígenes de entropía bloqueantes. | Bloqueo de procesos criptográficos y generación de sesiones, parálisis. | Uso de algoritmos de CSPRNG robustos y no bloqueantes basados en estado (ej. ChaCha20). |
| ADV-043 | Secuestro de Variables de Sesión Web (Session Hijacking) | Interfaces Web (DOM) | Robo de tokens a través de XSS u otras vulnerabilidades inyectadas en la interfaz. | Suplantación del operador y mutación de estado arbitraria. | Cookies `HttpOnly`, `Secure`, validación de IP/User-Agent y tiempo de vida estricto (TTL). |
| ADV-044 | Contrabando de Peticiones HTTP (HTTP Request Smuggling) | Balanceadores de Carga y Proxies | Explotación de inconsistencias en el parseo de los encabezados `Content-Length` y `Transfer-Encoding`. | Bypass de controles de seguridad y envenenamiento del caché del balanceador. | Normalización estricta y configuración uniforme del servidor web y proxy intermedio. |
| ADV-045 | Explotación de Deserialización Insegura | APIs de Comunicación de Objetos (Pickle/YAML) | Modificación de payloads serializados para instanciar clases arbitrarias o ejecutar comandos al deserializar. | RCE incondicional en el receptor. | Uso exclusivo de formatos de serialización inertes (JSON puro) o validación criptográfica del payload. |


<!-- Author: Borja Moskv (SYS_ID: borjamoskv) -->

### MATRIZ 5: VECTORES DE ATAQUE ADVERSARIAL (RED TEAMING) - BATCH 2 (016-030)
| ID | Vector Adversarial | Superficie de Ataque | Mecanismo de Explotación | Impacto Termodinámico | Defensa (Mitigación) |
|---|---|---|---|---|---|
| ADV-016 | Oscilación de Tipo Débil | Motor de Tipado (Python/TS) | Tránsito de `Any/unknown` que muta dinámicamente forzando reconversión constante. | Consumo infinito de ciclos en parsers y fallos de aserción en runtime. | Imposición de validación Pydantic/Zod en frontera, prohibición estricta de `Any`. |
| ADV-017 | Degradación Criptográfica por Entropía | Capa de Seguridad (Hashes/Tokens) | Uso de PRNGs predecibles en la generación de identificadores de sesión o estado. | Falsificación de colisiones y enrutamiento cruzado de contexto entre agentes. | Obligación del uso de `secrets` (CSPRNG) o UUIDv4 validados criptográficamente. |
| ADV-018 | Saturación de Descriptores de Archivo (FD Leak) | IO de Sistema Operativo | Apertura masiva de ficheros de log sin cierre (context manager ignorado). | Exhaustión de recursos del kernel (`Too many open files`), caída de Swarm. | Bloques `with` forzados a nivel AST; límite de FD a nivel contenedor/proceso. |
| ADV-019 | Fuga de Contexto por Variables Globales | Estado de Memoria Compartida | Manipulación de variables de módulo (singletons mutables) por agentes concurrentes. | Corrupción cruzada de estado y pérdida de determinismo C5. | Inmutabilidad forzada; estado inyectado siempre por dependencia (Dependency Injection). |
| ADV-020 | Interbloqueo de Pipes (Deadlock FIFO) | IPC (Inter-Process Communication) | Procesos leyendo/escribiendo mutuamente en pipes bloqueantes sin tamaño de buffer controlado. | Parálisis termodinámica (cero progreso, CPU ocupada). | I/O asíncrona estricta con timeouts y buffers limitados; fallback a Sockets UDP/TCP locales. |
| ADV-021 | Agotamiento del Thread Pool | Orquestador de Concurrencia | Ejecución de operaciones IO síncronas bloqueantes dentro de hilos asíncronos (event loop). | Inanición (Starvation) del orquestador, bloqueando otras tareas críticas. | Aislamiento de IO síncrona en `ThreadPoolExecutor` dedicados y supervisados. |
| ADV-022 | Inundación de Invocación Recursiva (Fork Bomb) | Motor de Subagentes (`invoke_subagent`) | Generación incontrolada de agentes hijos sin condición de parada topológica. | Colapso rápido de RAM y muerte térmica del CORTEX (OOM). | Límite estricto de profundidad en el grafo causal (Max Depth) y cuota de tokens/hijos. |
| ADV-023 | Interferencia por Variables de Entorno Ocultas | Entorno de Despliegue/Ejecución | Inyección silenciosa de configuración (ej. `PYTHONPATH` mutado) alterando resolución de módulos. | Fractura del Isomorfismo Causal; comportamiento no reproducible. | Purga y sobreescritura explícita del bloque `env` en cada invocación determinista. |
| ADV-024 | Degradación por Fallo Silencioso (Swallowed Exception) | Manejo de Errores P0/P1 | Bloques `try/except` globales que suprimen excepciones críticas sin registrar el estado de error. | Propagación de corrupción de estado y pérdida de rastreo de anomalías. | Prohibición absoluta de `except Exception: pass`. Requerimiento de logging y propagación Ouroboros. |
| ADV-025 | Inconsistencia de Relojes (Time Drift) | Sistemas Distribuidos / Cron | Divergencia en las marcas de tiempo entre nodos o subagentes al consolidar estado (C5). | Colisiones falsas en aserciones de mutación e invalidación errónea de caché. | Uso exclusivo de relojes lógicos (Vector Clocks, Lamport) o sincronización forzada NTP. |
| ADV-026 | Contaminación del AST por Macros (Template Injection) | Motores de Renderizado C4-SIM | Inyección de lógica no controlada en plantillas que afecta la estructura base al compilar. | Riesgo de ejecución arbitraria (RCE) y corrupción del modelo de dominio. | Sanitización absoluta y uso de sandboxing (ej. `jinja2.StrictUndefined`). |
| ADV-027 | Bloqueo por Adquisición de Locks Abandonados | Sincronización Concurrente | Un subagente o proceso muere reteniendo un lock (mutex/semáforo) en disco o memoria. | Congelamiento permanente del recurso crítico protegido. | Adquisición de locks siempre vinculada al ciclo de vida del proceso o locks con expiración (Leases). |
| ADV-028 | Varianza en Resolución de Dependencias (Phantom Dependency) | Gestores de Paquetes (pip/npm) | Ausencia de "lockfiles" estrictos que provoca descargas dispares según el estado de la red. | Divergencia en los entornos; imposibilidad de garantizar ejecución isomorfa (C5). | Verificación criptográfica (hashes) obligatoria en el `requirements.txt` o lockfile equivalente. |
| ADV-029 | Desalineamiento Semántico de Tokens (Token Drift) | Modelos de Lenguaje Invocados | Variación en la tokenización (cambios de modelo o versión) que rompe el parseo de estructuras (JSON). | Ruido termodinámico; caídas continuas en parsers y aumento de Anergía. | Parámetros de forzado estructural (`response_format` JSON puro) y validadores sintácticos robustos. |
| ADV-030 | Alteración Inducida del Grafo de Flujo (Control Flow Hijack) | Callbacks / Funciones de Orden Superior | Sustitución de referencias a callbacks por funciones no operativas mediante reflexión. | Pérdida del determinismo de la trayectoria (Agent Workflow). | Sellado de clases/módulos y restricción de inyección a interfaces inmutables. |

<!-- Author: Borja Moskv (SYS_ID: borjamoskv) -->

### [PRIMITIVA-SEO-2026] VANGUARDIA DE DOMINIO VECTORIAL (ULTRATHINK P0)
*Mapeo de Vectores de Ingesta RAG y Termodinámica de Búsqueda.*

| ID | Vector Adversarial (SEO) | Superficie de Ataque | Mecanismo de Explotación (Vector) | Impacto Termodinámico | Defensa / Ejecución C5-REAL |
|---|---|---|---|---|---|
| ADV-051 | Entropía por "Slop" (Anergía) | Motores RAG (Brave/Google) | Los motores descartan la prosa decorativa ("Green Theater"). Introducciones diluyen prominencia. | Ruido blanco; filtrado del índice por baja entropía de Shannon. | Colapsar contenido en HTML semántico crudo, YAML, JSON-LD, tablas y Markdown. |
| ADV-052 | Degradación de Isomorfismo Causal | Modelo Base C4-SIM | El motor ya posee conocimiento general. Si no se inyecta variable causal (First-Party Data), se ignora. | Ausencia de Information Gain; irrelevancia epistémica. | Exponer First-Party Data: logs empíricos, repositorios (Git Hashes), telemetría. Oráculo. |
| ADV-053 | Fricción de Ingesta DOM | Extracción de Sub-grafos | Los LLMs extraen fragmentos semánticos, no leen páginas. Diseño no optimizado bloquea RAG. | Parálisis de indexación de fracciones de respuesta. | `<h2>` Pregunta -> `<p>` Respuesta (<40 tokens densos). IDs únicos y descriptivos. |
| ADV-054 | Sumidero de Autoridad Epistémica | Grafo de Tolerancia Bizantina | Enlaces hacia sumideros entrópicos estocásticos destruyen la credibilidad matemática del nodo. | Caída del PageRank Bizantino; purga por asociación estocástica. | Anclaje estricto a repositorios verificables, firmas PGP/ENS y fuentes primarias (ArXiv). |
| ADV-055 | Asfixia por Límite Físico de Red | TTFB y Renderizado | Frameworks pesados (Next.js/Vercel) para contenido estático aumentan latencia (Invariante 11). | Penalización por ineficiencia termodinámica del Crawler. | Cloudflare Edge/Workers. Renderizado estático absoluto. TTFB < 50ms. |
| ADV-056 | Ceguera Semántica Estructural | Parser de Google AI / Brave | Extracción superficial CSS visual ignorando grafos relacionales RDF. | Pérdida de mapeo de relaciones causales en la red. | Inyección YAML/JSON-LD determinista (hasPart, isBasedOn). Anclaje ontológico denso. |
| ADV-057 | Resistencia de Extracción de Datos | Bot Autónomos / DOM RAG | Tráfico primario es de agentes extractores asfixiados por DOM dinámico estocástico o CSR. | Ignorado por Extractores; falla O(1) de parseo. | Interfaz dual isomorfa a API. Atributos `data-*` ricos y tablas de datos estructuradas ocultas. |
| ADV-058 | Muerte Térmica de Renderizado (CSR Death) | Crawl Budget (Motor) | React SPA puro fuerza evaluación estocástica de JS pesado, penalizando frecuencia de crawling. | Indexación retardada o parcial por límite termodinámico de Crawler. | Prohibición P0 del CSR de texto primario. SSG o SSR absoluto al primer byte. |
| ADV-059 | Dilución Epistémica en Modelos RAG | Compresión RAG / Texto Sintético | Contenido prolijo (Anergía narrativa) es resumido y destruido en su matriz de ganancia. | Supresión de fuente original en respuestas del LLM generativo. | Aplicación de Landauer: Semantic PoW. Jerga inyectada (C5) forzando cita directa inmutable. |
| ADV-060 | Invariancia Falsa de Frescura (Fake Freshness) | Indexadores de Motores (Sitemaps) | Falsificación de variables como `lastmod` sin alteración criptográfica del nodo de información. | Disminución del Crawl Rate. Estancamiento. | Exposición directa del Ledger: Git-Backed Signals (Diffs y Changelogs Puros BFT). |
| ADV-061 | OOM UI por Texturas Asíncronas | Degradación del DOM y GPU por sobrecarga gráfica de texturas asíncronas. | Renderizado de CSS/WebGL. | Saturación VRAM; Caída de FPS. | Monitoreo de framerate y Apoptosis visual. |
| ADV-062 | Fricción Cognitiva Extrema | Fricción cognitiva extrema en workflows donde la estética rompe la affordance (Usabilidad degradada). | Flujos de usuario P0. | Confusión de UI; Anergía operatoria. | Aserción de Affordance BFT. |
| ADV-063 | Secuestro por Dependencia Opaca | Cadena de Suministro / Abstracción | Una abstracción interna cambia su comportamiento asintótico de forma silenciosa. | Agotamiento térmico y caída del sistema bajo carga sin romper las interfaces formales. | Anclaje de hashes a nivel AST o congelación de dependencias (Lockfile Tear mitigado). |
