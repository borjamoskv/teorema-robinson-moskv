{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "Anergy-OMEGA I/O Contract",
  "type": "object",
  "properties": {
    "intent": {
      "type": "string",
      "description": "The triggering intent, e.g., 'purge dead code' or 'detect anergy'."
    },
    "target_directory": {
      "type": "string",
      "description": "The absolute path to scan for anergy.",
      "default": "$CORTEX_ROOT/10_PROJECTS/cortex-persist"
    },
    "purge_mode": {
      "type": "string",
      "enum": ["dry_run", "c5_real_destructive"],
      "description": "Must be c5_real_destructive for actual annihilation."
    }
  },
  "required": ["intent", "target_directory", "purge_mode"]
}
