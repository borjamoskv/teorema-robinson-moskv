{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "Exergy-Matrix-OMEGA I/O Contract",
  "type": "object",
  "properties": {
    "intent": {
      "type": "string",
      "description": "The triggering intent, e.g., 'audit my schedule' or 'find anergy in my day'."
    },
    "input_data": {
      "type": "string",
      "description": "The text, log, or schedule to be audited against the Exergy Matrix."
    }
  },
  "required": ["intent", "input_data"]
}
